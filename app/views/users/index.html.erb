<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if @current_user.present? %>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.location %></td>
          <td><%= link_to 'View Profile', user %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>
